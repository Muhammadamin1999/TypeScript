const Random_wait_time = Math.round(Math.random()*500) + 500
let startTime = new Date()
let endTime  // its type will be any

setTimeout(() => {
    endTime = 0
    endTime = new Date()
}, Random_wait_time);